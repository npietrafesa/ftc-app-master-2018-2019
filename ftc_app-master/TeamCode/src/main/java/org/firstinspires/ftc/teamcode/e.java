package org.firstinspires.ftc.teamcode;

import com.qualcomm.robotcore.eventloop.opmode.OpMode;
import com.qualcomm.robotcore.eventloop.opmode.TeleOp;
import com.qualcomm.robotcore.hardware.DcMotor;

//place @Autonomous or @Teleop above class to show where it would appear on phone.
//do not forget to extend the class off of OpMode or LinearOpMode, depending on if it is teleop or autonomous, respectively.erta
//once it is extended, remember to implement methods so the init() and loop() methods appear
public class e {
    //call variables here, above init() (keep in mind they are case sensitive, make sure you are typing the right thing.)
    //usually, the first letters of each word is capitalized (ex. DcMotor, OpMode, ColorSensor, etc.)
    //map the variable onto the phones inside the init()
    //in the loop, map the variables to the controller (gamepad1 or gamepad2)
}
